# React Native 0.77 迁移指南

> 状态：⚠️ **进行�?*�?026-05-24�? 
> 迁移路径：`0.74.7` �?`0.77.3`  
> 参考：[RN 0.77 发布说明](https://reactnative.dev/blog/2025/01/21/version-0.77) · [Upgrade Helper](https://react-native-community.github.io/upgrade-helper/?from=0.74.7&to=0.77.3)

---

## 当前进度总览

| 阶段 | 状�?| 说明 |
|------|------|------|
| 依赖版本升级 | �?已完�?| `package.json` / `pnpm-lock.yaml` |
| Android Gradle 配置 | �?已完�?| settings / build / wrapper / autolinking |
| iOS AppDelegate | �?已完�?| 集成 `RCTAppDependencyProvider` |
| `pnpm install` | �?已完�?| RN CLI 15.0.1，Metro 可识�?0.77.3 |
| Metro 开发服务器 | �?已验�?| `pnpm dev:mobile`（需�?monorepo 根目录启动） |
| Webpack H5 | �?已验�?| `pnpm dev:web` 独立运行 |
| Android 真机/模拟器编�?| �?待验�?| 需在本地执�?`pnpm android` |
| iOS Pod 安装与编�?| �?待验�?| 需�?macOS 执行 `cd ios && pod install` |
| RNW（Rspack）构�?| �?待验�?| `pnpm dev:mobile:web` |
| TypeScript 全量通过 | �?已修�?| 2026-05-24 修复 Scanner / jsbridge 路径 / 导航类型 |
| 新架构（Fabric）启�?| ⏸️ 未启�?| 保持 `newArchEnabled=false` |
| 文档�?README 同步 | �?本文�?| �?|

**整体完成度：�?80%**（代码、配置、typecheck 已完成，原生平台编译与回归测试待完成�?

---

## 版本对照

### 核心运行�?

| �?| 迁移�?| 迁移�?|
|----|--------|--------|
| `react-native` | 0.74.7 | **0.77.3** |
| `react` | 18.2.0 | **18.3.1** |
| `react-dom` | ^18.2.0 | **18.3.1** |
| `react-test-renderer` | 18.2.0 | **18.3.1** |

> RN 0.77 仍使�?**React 18**（React 19 �?0.78 起引入）�?

### 工具�?

| �?| 迁移�?| 迁移�?|
|----|--------|--------|
| `@react-native-community/cli` | 13.6.9 | **15.0.1** |
| `@react-native/babel-preset` | 0.74.89 | **0.77.3** |
| `@react-native/metro-config` | 0.74.89 | **0.77.3** |
| `@react-native/eslint-config` | 0.74.89 | **0.77.3** |
| `@react-native/typescript-config` | 0.74.89 | **0.77.3** |
| `@react-native/gradle-plugin` | 0.74.89（显式依赖） | �?`react-native` 内置，已从根 devDeps 移除 |

### 原生相关�?

| �?| 迁移�?| 迁移�?| 备注 |
|----|--------|--------|------|
| `react-native-screens` | 3.37.0 | **4.10.0** | 固定版本，避�?4.25+ 要求 RN �?.82 |
| `react-native-safe-area-context` | ^4.10.0 | ^4.14.0 | �?|
| 其余 RN 生态库 | 未变 | 未变 | navigation / webview / async-storage �?|

### Android 构建环境

| �?| 迁移�?| 迁移�?|
|----|--------|--------|
| Gradle | 8.5 | **8.10.2** |
| compileSdk | 34 | **35** |
| minSdk | 23 | **24** |
| targetSdk | 34 | 34 |
| Kotlin | 1.9.22 | **2.0.21** |
| NDK | 26.1.x | **27.1.12297006** |
| buildTools | 34.0.0 | **35.0.0** |

---

## 已修改文件清�?

### JavaScript / 配置

| 文件 | 变更摘要 |
|------|----------|
| `package.json` | RN 0.77.3、React 18.3.1、CLI 15、screens 4.10.0 |
| `apps/mobile/package.json` | Metro �?monorepo 根目录启动（`pnpm --dir ../.. exec react-native start`�?|
| `apps/web/webpack.config.js` | 配置迁至 `apps/web`，workspace alias，webpack 5.107.0 统一 |
| `pnpm-lock.yaml` | 锁定新版本树 |

### Android

| 文件 | 变更摘要 |
|------|----------|
| `android/settings.gradle` | RN 0.77 �?autolinking（`ReactSettingsExtension.autolinkLibrariesFromCommand()`�?|
| `android/build.gradle` | SDK 35、Kotlin 2.0、NDK 27；移�?`allprojects` |
| `android/app/build.gradle` | `autolinkLibrariesWithApp()`；移除旧 `native_modules.gradle`；保�?monorepo `root` / `entryFile` |
| `android/gradle/wrapper/gradle-wrapper.properties` | Gradle 8.10.2 |
| `android/gradle.properties` | **未改** `newArchEnabled=false`，保�?Hermes 与自定义 JVM 参数 |

### iOS

| 文件 | 变更摘要 |
|------|----------|
| `ios/MyRnApp/AppDelegate.mm` | 新增 `RCTAppDependencyProvider`�?.77 第三方依赖加载必需�?|
| `ios/Podfile` | 结构兼容 0.77，保�?`MyRnAppTests` target |

---

## Monorepo 特殊说明

本项目为 **pnpm workspace + Nx**，原生工程在仓库根目录（`android/`、`ios/`），JS 入口在根目录 `index.js`，应用源码在 `apps/mobile/`�?

### Metro 必须从根目录启动

RN CLI �?`apps/mobile` 子目录下无法注册 `start` 命令（缺�?`android/`、`ios/`）。已�?`apps/mobile/package.json` 中处理：

```json
"dev": "pnpm --dir ../.. exec react-native start --no-interactive"
```

### Webpack / Rspack 不受 RN 升级直接影响

| 路径 | 工具 | 配置位置 |
|------|------|----------|
| `@myapp/web` | Webpack 5.107.0 | `apps/web/webpack.config.js` |
| `@myapp/mobile` RNW | Rspack | 根目�?`rspack.config.js` |
| 原生 App | Metro | 根目�?`metro.config.js` |

---

## 本地验证步骤

### 1. 安装依赖

```bash
pnpm install
```

### 2. 清理缓存（升级后建议执行一次）

```bash
pnpm clean:cache
```

### 3. Metro

```bash
pnpm dev:mobile
```

### 4. Android

```bash
pnpm android
# �?
pnpm dev:android
```

**注意**：minSdk 已升�?**24**，不再支�?Android 7.0 以下设备�?

### 5. iOS（macOS�?

```bash
cd ios && pod install && cd ..
pnpm ios
```

�?Pod 安装失败，尝试：

```bash
cd ios
rm -rf Pods Podfile.lock build
pod install --repo-update
```

### 6. Web 回归

```bash
pnpm dev:web          # 独立 H5
pnpm dev:mobile:web      # App RNW
pnpm typecheck        # TypeScript
```

---

## 已知问题与待�?

### 待验�?

- [ ] Android Debug / Release 完整编译
- [ ] iOS Debug / Release 完整编译（含 `RCTAppDependencyProvider` Pod�?
- [ ] 真机 JSBridge、WebView、导航、媒体保存等功能回归
- [ ] Rspack RNW 路径�?0.77 下是否正�?

### TypeScript（已修复 2026-05-24�?

`pnpm typecheck` 现已 4/4 通过。主要修复：

| 类别 | 修复方式 |
|------|----------|
| `Scanner` 模块 | 新增 `Scanner.tsx` 桥接，修�?`index.ts` �?`ScannerScreen` 导入 |
| `useScanner` 类型 | 导出 `UseScannerOptions` / `UseScannerReturn` |
| `@myapp/jsbridge` 解析 | `tsconfig.base.json` 与各 app 增加 paths 指向 `packages/jsbridge/src` |
| Bridge 类型 | App/Web �?`@myapp/jsbridge` 导入，不再从 shared |
| `Window.ReactNativeWebView` | 修复 `globals.d.ts`，移除错�?import |
| 导航类型 | `RootStackParamList` + `NavigationContainer` 泛型 + `CommonActions.navigate` |
| `HybridWebView` | 排除�?WebView 冲突�?`renderError` / `renderLoading` props |

### 未纳入本次迁�?

- **新架构（Fabric + TurboModules�?*：仍�?`newArchEnabled=false`
- **React 19**：需等到 RN 0.78+
- **Expo**：本项目�?Expo 工作�?
- **Gemfile CocoaPods 版本约束**：仍为旧模板，若 iOS pod 报错再更�?

---

## 回滚方案

若需回退�?0.74.7�?

```bash
git checkout -- package.json pnpm-lock.yaml android/ ios/ apps/mobile/package.json
pnpm install
cd ios && pod install && cd ..
```

建议在回滚前�?tag：`git tag pre-rn-077`.

---

## RN 0.77 主要变化（与本项目相关）

| 变化 | 影响 |
|------|------|
| Android autolinking 重构 | `settings.gradle` + `autolinkLibrariesWithApp()` |
| `RCTAppDependencyProvider` | iOS 必须集成，否则第三方原生模块可能运行时异�?|
| minSdk 24 | 丢弃 API 23 设备 |
| Kotlin 2.0 | �?AGP 8.7+ 配套 |
| CLI 15.x | `react-native start/run-android` 行为�?13.x 略有差异 |
| 默认新架构模�?| 本项�?*刻意保持关闭**，降低迁移风�?|

更多版本背景�?[A04-各大版本之间的区别](./A04-各大版本之间的区�?md)�?

---

## 变更日志

| 日期 | 内容 |
|------|------|
| 2026-05-24 | 启动 0.74.7 �?0.77.3 迁移；完成依赖、Android、iOS AppDelegate、Webpack 生态整�?|
| 2026-05-24 | 修复全量 `pnpm typecheck`�?/4 通过�?|
