让我查看项目中应用内购买的实现：

用户问的是"应用内购买如何实现"。让我先查看项目中现有的应用内购买实现，然后详细说明。

从之前看到的代码中，我注意到项目使用了 `expo-in-app-purchases` 包。让我搜索相关的实现代码。

让我查看项目中应用内购买的具体实现：

项目使用了两个 IAP 库：

1. `expo-in-app-purchases` - Expo 提供的 IAP 库
2. `react-native-iap` - 另一个流行的 React Native IAP 库

让我查看具体的实现代码。

让我继续查看更多应用内购买的实现细节：

用户问的是"应用内购买如何实现"，我已经查看了项目中应用内购买的实现代码。让我继续查看更多相关代码来完整理解实现方式。

让我继续查看 IAP 的初始化和支付回调处理：

用户想了解应用内购买如何实现。我已经查看了项目中的相关代码，现在让我继续查看更多关键实现细节，特别是 useInit 中的 IAP 初始化和支付监听器的实现。

让我继续查看 IAP 初始化和回调处理的代码：

让我继续查看 useInit.tsx 中关于 IAP 初始化的部分，应该在后面的代码中。我需要找到 IAP 的初始化和购买回调处理逻辑。

用户想了解应用内购买（IAP）如何实现。我已经收集了足够的代码信息，现在可以总结项目中的 IAP 实现方式了。

从代码中可以看到：

1. 项目同时使用了两个 IAP 库：

   - `expo-in-app-purchases` - Expo 提供的 IAP
   - `react-native-iap` - 更通用的 React Native IAP 库

2. 支付流程：

   - iOS 使用 Apple Pay（IAP）
   - Android 使用支付宝等第三方支付
   - Web 端使用表单提交支付

3. 初始化流程在 `useInit.tsx` 中，包括：

   - 初始化 IAP 连接
   - 设置购买监听器
   - 处理购买回调

4. 充值页面在 `ChargeCenter.tsx` 中：

   - 获取商品列表
   - 请求购买
   - 处理支付结果

让我整理一个完整的总结。

# 💰 React Native 应用内购买实现详解

基于你的项目代码，我来详细说明应用内购买（IAP）的实现方式：

---

## 📦 使用的 IAP 库

你的项目同时使用了两个 IAP 库：

| 库                        | 用途                  | 安装                           |
| ------------------------- | --------------------- | ------------------------------ |
| **expo-in-app-purchases** | Expo 提供的 IAP 封装  | `expo-in-app-purchases@12.1.0` |
| **react-native-iap**      | 原生 React Native IAP | `react-native-iap@12.4.12`     |

---

## 🔧 IAP 初始化流程

### 初始化代码 - [useInit.tsx](file:///Users/tianhe/work/hqfront/app/hooks/useInit.tsx)

```typescript
import {
  IAPResponseCode,
  InAppPurchase,
  connectAsync,
  finishTransactionAsync,
  setPurchaseListener,
} from 'expo-in-app-purchases';
import {
  ProductPurchase,
  PurchaseError,
  SubscriptionPurchase,
  finishTransaction,
  initConnection,
  purchaseErrorListener,
  purchaseUpdatedListener,
} from 'react-native-iap';

// 登录成功后初始化 Apple Pay
const doTokenChangeInit = async () => {
  // ... 其他初始化逻辑
  if (Platform.OS === 'ios') {
    applePayinit(); // 初始化苹果支付
  }
};
```

**初始化步骤**：

1. 应用启动时调用 `initConnection()` 建立连接
2. 设置购买监听器 `purchaseUpdatedListener` 和 `purchaseErrorListener`
3. 用户登录成功后初始化 Apple Pay

---

## 🛒 支付流程实现

### 1. 获取商品列表 - [ChargeCenter.tsx](file:///Users/tianhe/work/hqfront/app/screens/ChargeCenter/ChargeCenter.tsx)

```typescript
const getIosProduct = async () => {
  if (Platform.OS === 'ios') {
    try {
      const products = await getProducts({
        skus: result?.result?.list?.map(e => String(e?.appleGoodsId)),
      });
      if (products.length === 0) {
        setfetchIOSProduct(false);
        showError('获取苹果购买物品为空!');
      } else {
        setfetchIOSProduct(true);
      }
    } catch (err) {
      showError(err?.message || err?.name);
      setfetchIOSProduct(false);
    }
  } else {
    setfetchIOSProduct(true);
  }
};
```

**关键说明**：

- 从服务端获取商品配置（包含 `appleGoodsId`）
- 使用 `getProducts()` 获取 App Store 中的商品信息
- 验证商品是否存在

### 2. 创建订单并发起支付

```typescript
const goCheckOut = async () => {
  // 1. 创建订单
  const orderRes = await accountStore.environment.api.normalRequest(
    '/user/game/createDiamondOrder',
    {platform: pm, id: currentChoose?.id},
    'post',
  );

  if (orderRes.kind === 'ok') {
    await startPaywayPay(orderRes.data);
  }
};

const startPaywayPay = async (passOrderId: string) => {
  // 苹果支付 (payway === 3)
  if (payway === 3) {
    showLoad('连接Apple store...');
    const applePayres = await requestPurchase({
      sku: String(currentChoose?.appleGoodsId),
    });
    // 支付结果由监听器处理
  }
  // 支付宝等其他支付方式...
};
```

### 3. 支付方式选择 - [PayComponent.tsx](file:///Users/tianhe/work/hqfront/app/screens/ChargeCenter/Components/PayComponent.tsx)

```typescript
export const PayComponent: React.FC<PayComponentProps> = ({onChoosePayWay}) => {
  const {result} = useApi<paylist>({
    api: {
      path: '/user/pay/getAvailablePaylist',
      params: {platform: Platform.OS, usage: 2},
    },
  });

  // 服务端返回可用支付方式列表
  // iOS: Apple Pay、支付宝、微信
  // Android: 支付宝、微信、银行卡等
  // Web: 支付宝、微信H5支付
};
```

---

## 📱 平台差异处理

### 支付方式对比

| 平台        | 支付方式  | 实现方式                   |
| ----------- | --------- | -------------------------- |
| **iOS**     | Apple Pay | `react-native-iap`         |
| **iOS**     | 支付宝    | Alipay SDK / WebView       |
| **Android** | 支付宝    | `react-native-ali-onepass` |
| **Android** | 微信支付  | `react-native-wechat`      |
| **Web**     | 支付宝    | 表单提交 / H5 跳转         |
| **Web**     | 微信支付  | 微信 JSAPI / 小程序支付    |

### 平台特定代码示例

```typescript
const pm = Platform.OS === 'ios' ? 'ios' : 'android';

// 苹果支付
if (payway === 3) {
  const applePayres = await requestPurchase({
    sku: String(currentChoose?.appleGoodsId),
  });
}
// 支付宝 - 原生
else if (payway === 1 && (Platform.OS === 'ios' || Platform.OS === 'android')) {
  navigation.navigate('AliWebView', {
    uri: payInfoRes?.data?.payInfo,
    title: '支付',
    ext: passOrderId,
  });
}
// 支付宝 - Web
else if (payway === 1 && Platform.OS === 'web') {
  const div = document.createElement('div');
  div.innerHTML = payInfoRes?.data?.payInfo;
  document.body.appendChild(div);
  document.forms[0].submit();
}
```

---

## 🎯 完整支付流程图

```
用户选择充值金额
      ↓
发起创建订单请求 (createDiamondOrder)
      ↓
选择支付方式 (Apple Pay / 支付宝 / 微信)
      ↓
┌─────────────────────────────────────────────────┐
│              苹果支付流程                        │
│  requestPurchase(sku)                          │
│       ↓                                        │
│  purchaseUpdatedListener 监听支付结果           │
│       ↓                                        │
│  finishTransaction() 完成交易                   │
│       ↓                                        │
│  服务端验证订单 (checkOrder)                    │
└─────────────────────────────────────────────────┘
      ↓
更新用户钻石余额
```

---

## ⚠️ 注意事项

### 1. 订单验证

```typescript
// 支付成功后必须验证订单
const checkOrder = async (orderId: string) => {
  const res = await accountStore.environment.api.normalRequest(
    '/user/game/checkDiamondOrder',
    {orderId},
    'post',
  );
  if (res.kind === 'ok') {
    // 更新余额
    showInfo('充值成功');
  }
};
```

### 2. 交易完成

```typescript
// 必须调用 finishTransaction 完成交易
// 否则会导致重复购买
finishTransaction({purchase: purchaseData});
```

### 3. 错误处理

```typescript
purchaseErrorListener((error: PurchaseError) => {
  console.warn('purchaseErrorListener', error);
  showError(error.message);
});
```

---

## 📝 配置要点

### iOS 配置

1. 在 App Store Connect 创建商品（SKU）
2. 配置 `SKProductsRequest`
3. 设置沙盒测试账号

### Android 配置

1. 在 Google Play Console 创建商品
2. 配置 BillingClient
3. 设置测试账号

### Web 配置

1. 配置支付宝/微信商户号
2. 设置回调地址
3. 处理跨域问题

---

## 🔄 支付状态流转

```
待支付 → 支付中 → 支付成功 → 订单验证 → 完成
              ↓
           支付失败
              ↓
           重试/取消
```

你的项目已经实现了完整的应用内购买流程，涵盖了 iOS、Android 和 Web 三个平台！
