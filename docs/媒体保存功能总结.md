# React Native 媒体保存功能

> 状态：⚠️ 部分实现  
> 最后同步：2026-05-24

## 当前实现

| 平台 | 状�?| 说明 |
|------|------|------|
| Web（`@myapp/web`�?| �?| `useSaveMedia.web.ts` 返回 `isSupported: false` |
| Web（App RNW�?| ⚠️ | 默认 hook 使用浏览器下�?API |
| Android | �?| 未接�?`@react-native-camera-roll/camera-roll` |
| iOS | �?| 未接�?CameraRoll |

## 已有代码

| 文件 | 说明 |
|------|------|
| `packages/shared/src/hooks/useSaveMedia.ts` | Hook 接口 + 浏览器下载逻辑 |
| `packages/shared/src/hooks/useSaveMedia.web.ts` | Web 平台 stub（不支持�?|
| `apps/mobile/src/screens/MediaSaveTestScreen.tsx` | 测试页（底部 Tab「Media」） |
| `android/app/src/main/AndroidManifest.xml` | 已声明存储权�?|

## Hook 用法

```typescript
import { useSaveMedia } from '@myapp/shared';

const { saveMedia, isLoading, error, success, reset } = useSaveMedia();

await saveMedia('https://example.com/image.jpg', { type: 'photo' });
```

## �?待实现（Native�?
1. 安装 `@react-native-camera-roll/camera-roll`
2. �?`useSaveMedia.ts` 中按平台分支�?   - Android：请�?`READ_MEDIA_IMAGES` / `WRITE_EXTERNAL_STORAGE` 权限
   - iOS：调�?`CameraRoll.save()`
3. 更新 `useSaveMedia.web.ts` 或在 RNW 环境启用浏览器下�?
参考方案见 [Android测试指南](./Android测试指南.md)�?
## 测试�?
```bash
pnpm dev:mobile
pnpm android
# 底部导航 �?Media 标签
```

> **注意**：Native 端当前无法正常保存到相册，测试页主要用于验证 UI �?Hook 接口�?
## 相关文档

- [Android 测试指南](./Android测试指南.md)
- [完整测试场景](./完整测试场景.md)
