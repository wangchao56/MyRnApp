# 🚀 媒体保存功能 - 快速测试卡�?
## 当前状�?
### �?Web 测试
- **URL**: http://localhost:3000/
- **状�?*: �?运行�?- **功能**: 直接下载到本�?
### �?Android 测试
- **Metro Bundler**: �?运行�?(端口 8081)
- **设备连接**: �?需要手动连�?- **状�?*: 等待测试

## 快速命�?
### 启动测试（Windows�?
```bash
# 1. Web 测试（已在运行）
# 访问 http://localhost:3000/

# 2. Android 测试
cd e:\workspace\MyRnApp
pnpm dev:mobile    # 启动 Metro Bundler
pnpm android    # 运行 Android 应用（需要设备）

# 3. 或者使用测试脚�?scripts\test-android-media.bat
```

### 检查设�?
```bash
# 查看已连接的 Android 设备
adb devices

# 示例输出�?# List of devices attached
# emulator-5554    device
# 0123456789ABCDEF  device
```

## 测试步骤

### Web 测试（已完成 ✅）

1. 打开 http://localhost:3000/
2. 滚动�?"Save Media Test" 卡片
3. 点击 "保存图片" �?"保存视频"
4. 文件会自动下�?
### Android 测试（待测试 ⏳）

1. 连接 Android 设备或启动模拟器
2. 运行 `pnpm android`
3. 应用启动后，点击底部 "Media" 标签
4. 点击 "📷 保存图片" �?"🎬 保存视频"
5. 授予存储权限（首次）
6. 查看相册确认保存

## 核心文件

### Hook 实现
📁 `packages/shared/src/hooks/useSaveMedia.ts`

### 测试页面
📁 `apps/mobile/src/screens/MediaSaveTestScreen.tsx`

### 权限配置
📁 `android/app/src/main/AndroidManifest.xml`

## API 使用

```typescript
import { useSaveMedia } from '@myapp/shared';

const { saveMedia, isLoading, error, success } = useSaveMedia();

// 保存图片
await saveMedia('https://example.com/image.jpg', {
  type: 'photo',
  album: 'MyApp'
});

// 保存视频
await saveMedia('https://example.com/video.mp4', {
  type: 'video',
  album: 'MyApp'
});
```

## 状态检�?
| 状�?| 含义 | 显示 |
|------|------|------|
| `isLoading` | 正在保存 | 加载动画 |
| `success` | 保存成功 | �?绿色提示 |
| `error` | 保存失败 | �?红色错误 |

## 权限要求

### Android

| 版本 | 必需权限 |
|------|---------|
| Android 13+ (API 33+) | `READ_MEDIA_IMAGES`, `READ_MEDIA_VIDEO` |
| Android 10-12 | `WRITE_EXTERNAL_STORAGE` |
| Android 9 及以�?| `WRITE_EXTERNAL_STORAGE`, `READ_EXTERNAL_STORAGE` |

### iOS
- 无需额外权限请求

### Web
- 无需权限

## 保存位置

- **相册名称**: `MyRnApp Test`
- **Android 路径**: `DCIM/MyRnApp Test/`
- **iOS**: 保存到相机胶卷的指定相册

## 故障排查

### �?Metro Bundler 未启�?```bash
cd e:\workspace\MyRnApp
pnpm dev:mobile
```

### �?Android 设备未检测到
```bash
# 检�?ADB
adb devices

# 重启 ADB 服务
adb kill-server
adb start-server
```

### �?权限被拒�?- 检�?`AndroidManifest.xml`
- 手动在设置中授予权限
- 尝试卸载重装应用

### �?保存失败
- 检查网络连接（测试 URL 需要网络）
- 查看终端错误日志
- 确认 `@react-native-camera-roll/camera-roll` 已安�?
## 测试 URL

- **测试图片**: `https://picsum.photos/800/600?random=100`
- **测试视频**: `https://www.w3schools.com/html/mov_bbb.mp4`

## 相关文档

📖 [完整实现总结](./媒体保存功能总结.md)
📖 [Android 测试指南](./Android测试指南.md)
📖 [useSaveMedia Hook 源码](../packages/shared/src/hooks/useSaveMedia.ts)

---

💡 **提示**: 测试完成后，别忘了查看保存的文件�?