{
  "name": "@myapp/app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@myapp/shared": "workspace:*",
    "@myapp/jsbridge": "workspace:*"
  }
}
