{
  "name": "@myapp/jsbridge",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/jsbridge/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": { "script": "build" }
    },
    "dev": {
      "executor": "nx:run-script",
      "options": { "script": "dev" }
    },
    "typecheck": {
      "executor": "nx:run-script",
      "options": { "script": "typecheck" }
    },
    "clean": {
      "executor": "nx:run-script",
      "options": { "script": "clean" }
    }
  }
}
